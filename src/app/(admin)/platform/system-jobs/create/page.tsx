/**
 * System Job Create Page (App Router)
 * 
 * Bridge to legacy page component
 * Pattern: App Router → Pages Directory (Single Source of Truth in /app)
 */

import AddSystemJobPage from '../../../../pages/AddSystemJobPage';

export default AddSystemJobPage;
