/**
 * Traffic Log Create Page (App Router)
 * 
 * Bridge to legacy page component
 * Pattern: App Router → Pages Directory (Single Source of Truth in /app)
 */

import AddTrafficLogPage from '../../../../pages/AddTrafficLogPage';

export default AddTrafficLogPage;
