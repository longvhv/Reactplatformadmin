-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.api_keys (
  _id uuid NOT NULL,
  tenant_id uuid NOT NULL,
  name text NOT NULL CHECK (length(name) > 0),
  key_prefix character varying NOT NULL,
  key_hash text NOT NULL UNIQUE,
  scopes ARRAY NOT NULL DEFAULT '{}'::text[],
  allowed_ips ARRAY,
  expires_at timestamp with time zone,
  last_used_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  version bigint NOT NULL DEFAULT 1 CHECK (version >= 1),
  CONSTRAINT api_keys_pkey PRIMARY KEY (_id),
  CONSTRAINT fk_api_key_tenant FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id),
  CONSTRAINT fk_api_key_creator FOREIGN KEY (created_by) REFERENCES public.users(_id)
);
CREATE TABLE public.app_capabilities (
  _id uuid NOT NULL DEFAULT gen_random_uuid(),
  tenant_id uuid NOT NULL,
  app_id uuid NOT NULL,
  code character varying NOT NULL,
  name character varying NOT NULL,
  description text,
  type character varying NOT NULL DEFAULT 'FEATURE'::character varying CHECK (type::text = ANY (ARRAY['FEATURE'::character varying, 'LIMIT'::character varying]::text[])),
  default_value jsonb NOT NULL DEFAULT '{}'::jsonb,
  display_order integer NOT NULL DEFAULT 0,
  is_required boolean NOT NULL DEFAULT false,
  validation_rules jsonb NOT NULL DEFAULT '{}'::jsonb,
  status character varying NOT NULL DEFAULT 'active'::character varying CHECK (status::text = ANY (ARRAY['active'::character varying, 'inactive'::character varying, 'archived'::character varying]::text[])),
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  version bigint NOT NULL DEFAULT 1 CHECK (version >= 1),
  CONSTRAINT app_capabilities_pkey PRIMARY KEY (_id)
);
CREATE TABLE public.applications (
  _id uuid NOT NULL DEFAULT gen_random_uuid(),
  code character varying NOT NULL UNIQUE CHECK (code::text ~ '^[A-Z0-9_]+$'::text),
  name character varying NOT NULL CHECK (length(name::text) > 0),
  description text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  version bigint NOT NULL DEFAULT 1 CHECK (version >= 1),
  CONSTRAINT applications_pkey PRIMARY KEY (_id)
);
CREATE TABLE public.article_types (
  _id uuid NOT NULL,
  app_code character varying NOT NULL,
  code character varying NOT NULL,
  name text NOT NULL,
  icon_url text,
  config_schema jsonb NOT NULL DEFAULT '{}'::jsonb,
  is_system boolean NOT NULL DEFAULT false,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT article_types_pkey PRIMARY KEY (_id),
  CONSTRAINT article_types_app_code_fkey FOREIGN KEY (app_code) REFERENCES public.applications(code)
);
CREATE TABLE telemetry.auth_logs (
  _id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid,
  tenant_id uuid,
  action character varying NOT NULL,
  status character varying NOT NULL,
  ip_address inet,
  user_agent text,
  browser character varying,
  os character varying,
  device_type character varying,
  location character varying,
  country_code character varying,
  error_message text,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT auth_logs_pkey PRIMARY KEY (_id),
  CONSTRAINT auth_logs_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id)
);
CREATE TABLE public.department_members (
  _id uuid NOT NULL DEFAULT uuid_generate_v4(),
  tenant_id uuid NOT NULL,
  department_id uuid NOT NULL,
  tenant_member_id uuid NOT NULL,
  is_primary boolean NOT NULL DEFAULT false,
  role_in_department character varying,
  joined_at timestamp with time zone,
  left_at timestamp with time zone,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  deleted_at timestamp with time zone,
  created_by uuid,
  updated_by uuid,
  deleted_by uuid,
  version bigint NOT NULL DEFAULT 1 CHECK (version >= 1),
  CONSTRAINT department_members_pkey PRIMARY KEY (_id),
  CONSTRAINT fk_dept_members_tenant FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id),
  CONSTRAINT fk_dept_members_department FOREIGN KEY (department_id) REFERENCES public.departments(_id),
  CONSTRAINT fk_dept_members_tenant_member FOREIGN KEY (tenant_member_id) REFERENCES public.tenant_members(_id)
);
CREATE TABLE public.departments (
  _id uuid NOT NULL DEFAULT uuid_generate_v4(),
  tenant_id uuid NOT NULL,
  code character varying NOT NULL,
  name character varying NOT NULL,
  parent_department_id uuid,
  manager_id uuid,
  description text,
  status character varying NOT NULL DEFAULT 'ACTIVE'::character varying CHECK (status::text = ANY (ARRAY['ACTIVE'::character varying, 'INACTIVE'::character varying, 'ARCHIVED'::character varying]::text[])),
  order integer DEFAULT 0,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  deleted_at timestamp with time zone,
  created_by uuid,
  updated_by uuid,
  deleted_by uuid,
  version bigint NOT NULL DEFAULT 1 CHECK (version >= 1),
  CONSTRAINT departments_pkey PRIMARY KEY (_id),
  CONSTRAINT fk_departments_tenant FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id),
  CONSTRAINT fk_departments_parent FOREIGN KEY (parent_department_id) REFERENCES public.departments(_id),
  CONSTRAINT fk_departments_manager FOREIGN KEY (manager_id) REFERENCES public.tenant_members(_id)
);
CREATE TABLE public.feature_flags (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  flag_key character varying NOT NULL UNIQUE,
  flag_name character varying NOT NULL,
  description text,
  is_enabled boolean DEFAULT false,
  environment character varying DEFAULT 'production'::character varying,
  flag_type character varying NOT NULL DEFAULT 'boolean'::character varying,
  target_audience character varying,
  percentage_rollout integer DEFAULT 0,
  conditions jsonb,
  metadata jsonb,
  created_by character varying,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  enabled_at timestamp with time zone,
  disabled_at timestamp with time zone,
  CONSTRAINT feature_flags_pkey PRIMARY KEY (id)
);
CREATE TABLE public.group_members (
  _id uuid NOT NULL DEFAULT uuid_generate_v4(),
  tenant_id uuid NOT NULL,
  user_group_id uuid NOT NULL,
  tenant_member_id uuid NOT NULL,
  is_primary boolean NOT NULL DEFAULT false,
  role_in_group character varying,
  joined_at timestamp with time zone,
  left_at timestamp with time zone,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  deleted_at timestamp with time zone,
  created_by uuid,
  updated_by uuid,
  deleted_by uuid,
  version bigint NOT NULL DEFAULT 1 CHECK (version >= 1),
  CONSTRAINT group_members_pkey PRIMARY KEY (_id),
  CONSTRAINT fk_group_members_tenant FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id),
  CONSTRAINT fk_group_members_user_group FOREIGN KEY (user_group_id) REFERENCES public.user_groups(_id),
  CONSTRAINT fk_group_members_tenant_member FOREIGN KEY (tenant_member_id) REFERENCES public.tenant_members(_id)
);
CREATE TABLE public.kv_store_01107967 (
  key text NOT NULL,
  value jsonb NOT NULL,
  CONSTRAINT kv_store_01107967_pkey PRIMARY KEY (key)
);
CREATE TABLE public.kv_store_408066bd (
  key text NOT NULL,
  value jsonb NOT NULL,
  CONSTRAINT kv_store_408066bd_pkey PRIMARY KEY (key)
);
CREATE TABLE public.kv_store_64d00b7b (
  key text NOT NULL,
  value jsonb NOT NULL,
  CONSTRAINT kv_store_64d00b7b_pkey PRIMARY KEY (key)
);
CREATE TABLE public.kv_store_6a2fdedc (
  key text NOT NULL,
  value jsonb NOT NULL,
  CONSTRAINT kv_store_6a2fdedc_pkey PRIMARY KEY (key)
);
CREATE TABLE public.kv_store_7eedb4e0 (
  key text NOT NULL,
  value jsonb NOT NULL,
  CONSTRAINT kv_store_7eedb4e0_pkey PRIMARY KEY (key)
);
CREATE TABLE public.kv_store_9a2f5d07 (
  key text NOT NULL,
  value jsonb NOT NULL,
  CONSTRAINT kv_store_9a2f5d07_pkey PRIMARY KEY (key)
);
CREATE TABLE public.kv_store_ab02637d (
  key text NOT NULL,
  value jsonb NOT NULL,
  CONSTRAINT kv_store_ab02637d_pkey PRIMARY KEY (key)
);
CREATE TABLE public.kv_store_db9bf4cf (
  key text NOT NULL,
  value jsonb NOT NULL,
  CONSTRAINT kv_store_db9bf4cf_pkey PRIMARY KEY (key)
);
CREATE TABLE public.legal_documents (
  _id uuid NOT NULL DEFAULT gen_random_uuid(),
  title character varying NOT NULL,
  slug character varying NOT NULL UNIQUE,
  type character varying NOT NULL CHECK (type::text = ANY (ARRAY['terms_of_service'::character varying, 'privacy_policy'::character varying, 'cookie_policy'::character varying, 'gdpr'::character varying, 'eula'::character varying, 'sla'::character varying, 'dpa'::character varying, 'other'::character varying]::text[])),
  version character varying NOT NULL DEFAULT '1.0'::character varying,
  content text NOT NULL,
  summary text,
  status character varying NOT NULL DEFAULT 'draft'::character varying CHECK (status::text = ANY (ARRAY['draft'::character varying, 'published'::character varying, 'archived'::character varying]::text[])),
  effective_date date,
  expiry_date date,
  tenant_id uuid,
  language character varying DEFAULT 'vi'::character varying,
  is_active boolean DEFAULT true,
  view_count integer DEFAULT 0,
  accept_count integer DEFAULT 0,
  created_by uuid,
  updated_by uuid,
  published_by uuid,
  published_at timestamp with time zone,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT legal_documents_pkey PRIMARY KEY (_id),
  CONSTRAINT fk_legal_documents_created_by_users FOREIGN KEY (created_by) REFERENCES public.users(_id),
  CONSTRAINT fk_legal_documents_updated_by_users FOREIGN KEY (updated_by) REFERENCES public.users(_id),
  CONSTRAINT fk_legal_documents_published_by_users FOREIGN KEY (published_by) REFERENCES public.users(_id),
  CONSTRAINT legal_documents_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id),
  CONSTRAINT legal_documents_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(_id),
  CONSTRAINT legal_documents_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(_id),
  CONSTRAINT legal_documents_published_by_fkey FOREIGN KEY (published_by) REFERENCES public.users(_id)
);
CREATE TABLE public.location_types (
  _id uuid NOT NULL,
  tenant_id uuid,
  code character varying NOT NULL CHECK (code::text ~ '^[A-Z0-9_]+$'::text),
  name text NOT NULL CHECK (length(name) > 0),
  description text,
  extra_fields jsonb NOT NULL DEFAULT '[]'::jsonb,
  is_system boolean NOT NULL DEFAULT false,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  version bigint NOT NULL DEFAULT 1 CHECK (version >= 1),
  CONSTRAINT location_types_pkey PRIMARY KEY (_id),
  CONSTRAINT fk_loctype_tenant FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id)
);
CREATE TABLE public.locations (
  _id uuid NOT NULL,
  tenant_id uuid NOT NULL,
  parent_id uuid,
  type_id uuid NOT NULL,
  name text NOT NULL CHECK (length(name) > 0),
  code character varying,
  path text,
  status character varying NOT NULL DEFAULT 'ACTIVE'::character varying CHECK (status::text = ANY (ARRAY['ACTIVE'::character varying, 'INACTIVE'::character varying, 'CLOSED'::character varying]::text[])),
  address jsonb NOT NULL DEFAULT '{}'::jsonb,
  coordinates point,
  radius_meters integer DEFAULT 100 CHECK (radius_meters > 0),
  timezone character varying NOT NULL DEFAULT 'UTC'::character varying,
  is_headquarter boolean NOT NULL DEFAULT false,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  deleted_at timestamp with time zone,
  version bigint NOT NULL DEFAULT 1,
  CONSTRAINT locations_pkey PRIMARY KEY (_id),
  CONSTRAINT fk_loc_tenant FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id),
  CONSTRAINT fk_loc_parent FOREIGN KEY (parent_id) REFERENCES public.locations(_id),
  CONSTRAINT fk_loc_type FOREIGN KEY (type_id) REFERENCES public.location_types(_id)
);
CREATE TABLE public.notification_templates (
  _id uuid NOT NULL DEFAULT gen_random_uuid(),
  tenant_id uuid NOT NULL,
  template_code character varying NOT NULL UNIQUE,
  template_name character varying NOT NULL,
  description text,
  subject character varying,
  body_text text,
  body_html text,
  notification_type character varying NOT NULL DEFAULT 'email'::character varying,
  category character varying,
  priority character varying DEFAULT 'normal'::character varying,
  language_code character varying DEFAULT 'vi'::character varying,
  variables jsonb DEFAULT '[]'::jsonb,
  sample_data jsonb,
  delivery_channels ARRAY DEFAULT ARRAY['email'::text],
  send_immediately boolean DEFAULT true,
  scheduled_send_time time without time zone,
  status character varying DEFAULT 'active'::character varying,
  is_system_template boolean DEFAULT false,
  is_editable boolean DEFAULT true,
  usage_count integer DEFAULT 0,
  last_used_at timestamp with time zone,
  success_count integer DEFAULT 0,
  failure_count integer DEFAULT 0,
  version integer DEFAULT 1,
  parent_template_id uuid,
  attachments jsonb,
  headers jsonb,
  metadata jsonb,
  tags ARRAY,
  created_at timestamp with time zone DEFAULT now(),
  created_by character varying,
  updated_at timestamp with time zone DEFAULT now(),
  updated_by character varying,
  deleted_at timestamp with time zone,
  deleted_by character varying,
  CONSTRAINT notification_templates_pkey PRIMARY KEY (_id),
  CONSTRAINT notification_templates_parent_template_id_fkey FOREIGN KEY (parent_template_id) REFERENCES public.notification_templates(_id)
);
CREATE TABLE public.permissions (
  _id uuid NOT NULL DEFAULT gen_random_uuid(),
  app_code character varying NOT NULL,
  code character varying NOT NULL UNIQUE CHECK (length(code::text) > 0),
  parent_code character varying,
  path text,
  is_group boolean NOT NULL DEFAULT false,
  name character varying NOT NULL CHECK (length(name::text) > 0),
  description text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  version bigint NOT NULL DEFAULT 1 CHECK (version >= 1),
  CONSTRAINT permissions_pkey PRIMARY KEY (_id),
  CONSTRAINT fk_perm_app FOREIGN KEY (app_code) REFERENCES public.applications(code),
  CONSTRAINT fk_perm_parent FOREIGN KEY (parent_code) REFERENCES public.permissions(code)
);
CREATE TABLE public.regions (
  _id uuid NOT NULL DEFAULT gen_random_uuid(),
  code character varying NOT NULL UNIQUE,
  name character varying NOT NULL,
  type character varying NOT NULL CHECK (type::text = ANY (ARRAY['REGION'::character varying, 'NATION'::character varying, 'PROVINCE'::character varying, 'DISTRICT'::character varying, 'COMMUNE'::character varying]::text[])),
  order integer DEFAULT 0,
  status smallint DEFAULT 1 CHECK (status = ANY (ARRAY[0, 1])),
  parent_id uuid,
  start_date date,
  end_date date,
  history_data jsonb DEFAULT '[]'::jsonb,
  metadata jsonb DEFAULT '{}'::jsonb,
  is_system boolean DEFAULT false,
  is_editable boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  version integer DEFAULT 1,
  CONSTRAINT regions_pkey PRIMARY KEY (_id),
  CONSTRAINT regions_parent_id_fkey FOREIGN KEY (parent_id) REFERENCES public.regions(_id)
);
CREATE TABLE public.reserved_slugs (
  _id uuid NOT NULL,
  slug character varying NOT NULL UNIQUE CHECK (slug::text ~ '^[a-z0-9-]+$'::text),
  type character varying NOT NULL DEFAULT 'SYSTEM'::character varying CHECK (type::text = ANY (ARRAY['SYSTEM'::character varying, 'BUSINESS'::character varying, 'OFFENSIVE'::character varying, 'FUTURE'::character varying]::text[])),
  match_type character varying NOT NULL DEFAULT 'EXACT'::character varying CHECK (match_type::text = ANY (ARRAY['EXACT'::character varying, 'PREFIX'::character varying, 'REGEX'::character varying]::text[])),
  items_snapshot jsonb NOT NULL DEFAULT '{}'::jsonb,
  reason text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  version bigint NOT NULL DEFAULT 1 CHECK (version >= 1),
  deleted_at timestamp with time zone,
  CONSTRAINT reserved_slugs_pkey PRIMARY KEY (_id)
);
CREATE TABLE public.roles (
  _id uuid NOT NULL DEFAULT gen_random_uuid(),
  tenant_id uuid NOT NULL,
  name character varying NOT NULL CHECK (length(name::text) > 0),
  description text,
  type character varying NOT NULL DEFAULT 'CUSTOM'::character varying CHECK (type::text = ANY (ARRAY['SYSTEM'::character varying, 'CUSTOM'::character varying]::text[])),
  permission_codes ARRAY NOT NULL DEFAULT '{}'::text[],
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  version bigint NOT NULL DEFAULT 1 CHECK (version >= 1),
  CONSTRAINT roles_pkey PRIMARY KEY (_id),
  CONSTRAINT roles_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id)
);
CREATE TABLE public.saas_product_types (
  _id uuid NOT NULL,
  code character varying NOT NULL UNIQUE CHECK (code::text ~ '^[A-Z0-9_]+$'::text),
  name text NOT NULL CHECK (length(name) > 0),
  description text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  version bigint NOT NULL DEFAULT 1 CHECK (version >= 1),
  CONSTRAINT saas_product_types_pkey PRIMARY KEY (_id)
);
CREATE TABLE public.saas_products (
  _id uuid NOT NULL DEFAULT gen_random_uuid(),
  tenant_id uuid NOT NULL,
  code character varying NOT NULL CHECK (code::text ~ '^[a-z0-9-]+$'::text),
  name character varying NOT NULL,
  description text,
  product_type_code character varying,
  base_price numeric NOT NULL DEFAULT 0 CHECK (base_price >= 0::numeric),
  currency character varying NOT NULL DEFAULT 'VND'::character varying,
  billing_cycle character varying NOT NULL DEFAULT 'MONTHLY'::character varying CHECK (billing_cycle::text = ANY (ARRAY['DAILY'::character varying, 'WEEKLY'::character varying, 'MONTHLY'::character varying, 'QUARTERLY'::character varying, 'YEARLY'::character varying, 'LIFETIME'::character varying]::text[])),
  trial_days integer NOT NULL DEFAULT 0 CHECK (trial_days >= 0),
  features jsonb NOT NULL DEFAULT '{}'::jsonb,
  limits jsonb NOT NULL DEFAULT '{}'::jsonb,
  status character varying NOT NULL DEFAULT 'active'::character varying CHECK (status::text = ANY (ARRAY['active'::character varying, 'inactive'::character varying, 'archived'::character varying]::text[])),
  is_featured boolean NOT NULL DEFAULT false,
  display_order integer NOT NULL DEFAULT 0,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  version bigint NOT NULL DEFAULT 1 CHECK (version >= 1),
  CONSTRAINT saas_products_pkey PRIMARY KEY (_id)
);
CREATE TABLE telemetry.security_audit_logs (
  _id uuid NOT NULL,
  tenant_id uuid,
  actor_id uuid,
  impersonator_id uuid,
  event_category text,
  event_action text,
  target_id uuid,
  resource_type text,
  ip_address inet,
  user_agent text,
  details jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT security_audit_logs_pkey PRIMARY KEY (_id)
);
CREATE TABLE public.service_accounts (
  _id uuid NOT NULL,
  tenant_id uuid NOT NULL,
  member_id uuid NOT NULL,
  name text NOT NULL CHECK (length(name) > 0),
  description text,
  client_id character varying NOT NULL UNIQUE,
  client_secret_hash text NOT NULL,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  version bigint NOT NULL DEFAULT 1 CHECK (version >= 1),
  CONSTRAINT service_accounts_pkey PRIMARY KEY (_id),
  CONSTRAINT fk_service_account_tenant FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id),
  CONSTRAINT fk_service_account_member FOREIGN KEY (member_id) REFERENCES public.tenant_members(_id)
);
CREATE TABLE public.service_packages (
  _id uuid NOT NULL DEFAULT gen_random_uuid(),
  package_code character varying NOT NULL,
  package_name character varying NOT NULL,
  product_id uuid NOT NULL,
  description text,
  billing_cycle USER-DEFINED NOT NULL DEFAULT 'MONTHLY'::billing_cycle_type,
  price numeric NOT NULL DEFAULT 0.00 CHECK (price >= 0::numeric),
  currency character varying NOT NULL DEFAULT 'USD'::character varying,
  features_config jsonb DEFAULT '[]'::jsonb,
  limits_config jsonb DEFAULT '{}'::jsonb,
  display_order integer DEFAULT 0 CHECK (display_order >= 0),
  is_public boolean DEFAULT true,
  is_active boolean DEFAULT true,
  tenant_id uuid NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT service_packages_pkey PRIMARY KEY (_id),
  CONSTRAINT service_packages_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.saas_products(_id)
);
CREATE TABLE public.storage_files (
  _id uuid NOT NULL,
  tenant_id uuid NOT NULL,
  parent_id uuid,
  is_folder boolean NOT NULL DEFAULT false,
  original_name text NOT NULL,
  storage_path text,
  public_url text,
  category character varying NOT NULL DEFAULT 'MEDIA'::character varying CHECK (category::text = ANY (ARRAY['MEDIA'::character varying, 'DOCUMENT'::character varying, 'ARCHIVE'::character varying, 'EXPORT'::character varying, 'SYSTEM'::character varying]::text[])),
  mime_type character varying NOT NULL,
  extension character varying,
  file_size bigint NOT NULL DEFAULT 0,
  items_snapshot jsonb NOT NULL DEFAULT '[]'::jsonb,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  storage_provider character varying NOT NULL DEFAULT 'S3'::character varying CHECK (storage_provider::text = ANY (ARRAY['S3'::character varying, 'R2'::character varying, 'MINIO'::character varying, 'CLOUDFLARE'::character varying]::text[])),
  visibility character varying NOT NULL DEFAULT 'PRIVATE'::character varying,
  status character varying NOT NULL DEFAULT 'PROCESSING'::character varying CHECK (status::text = ANY (ARRAY['UPLOADING'::character varying, 'PROCESSING'::character varying, 'READY'::character varying, 'FAILED'::character varying]::text[])),
  uploaded_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  deleted_at timestamp with time zone,
  version bigint NOT NULL DEFAULT 1 CHECK (version >= 1),
  CONSTRAINT storage_files_pkey PRIMARY KEY (_id),
  CONSTRAINT fk_storage_parent FOREIGN KEY (parent_id) REFERENCES public.storage_files(_id),
  CONSTRAINT fk_storage_tenant FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id)
);
CREATE TABLE public.subscription_invoices (
  _id uuid NOT NULL,
  tenant_id uuid NOT NULL,
  subscription_id uuid,
  order_id uuid,
  invoice_number character varying NOT NULL UNIQUE,
  status character varying NOT NULL DEFAULT 'DRAFT'::character varying CHECK (status::text = ANY (ARRAY['DRAFT'::character varying, 'OPEN'::character varying, 'PAID'::character varying, 'VOID'::character varying, 'UNCOLLECTIBLE'::character varying]::text[])),
  currency_code character varying NOT NULL DEFAULT 'VND'::character varying CHECK (length(currency_code::text) = 3),
  subtotal numeric NOT NULL DEFAULT 0,
  tax_amount numeric NOT NULL DEFAULT 0,
  discount_amount numeric NOT NULL DEFAULT 0,
  total_amount numeric NOT NULL DEFAULT 0,
  amount_paid numeric NOT NULL DEFAULT 0,
  amount_due numeric DEFAULT (total_amount - amount_paid),
  billing_info jsonb NOT NULL DEFAULT '{}'::jsonb,
  items_snapshot jsonb NOT NULL DEFAULT '[]'::jsonb,
  tax_breakdown jsonb NOT NULL DEFAULT '[]'::jsonb,
  billing_period_start timestamp with time zone NOT NULL,
  billing_period_end timestamp with time zone NOT NULL,
  due_date timestamp with time zone NOT NULL,
  paid_at timestamp with time zone,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  price_adjustments jsonb NOT NULL DEFAULT '[]'::jsonb,
  pdf_url text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  deleted_at timestamp with time zone,
  version bigint NOT NULL DEFAULT 1,
  CONSTRAINT subscription_invoices_pkey PRIMARY KEY (_id),
  CONSTRAINT fk_inv_tenant FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id),
  CONSTRAINT fk_inv_sub FOREIGN KEY (subscription_id) REFERENCES public.tenant_subscriptions(_id)
);
CREATE TABLE public.subscription_orders (
  _id uuid NOT NULL,
  tenant_id uuid NOT NULL,
  created_by uuid,
  order_number character varying NOT NULL UNIQUE,
  po_number character varying,
  type character varying NOT NULL DEFAULT 'NEW'::character varying CHECK (type::text = ANY (ARRAY['NEW'::character varying, 'RENEWAL'::character varying, 'UPGRADE'::character varying, 'DOWNGRADE'::character varying, 'ADD_ON'::character varying]::text[])),
  status character varying NOT NULL DEFAULT 'PENDING'::character varying CHECK (status::text = ANY (ARRAY['DRAFT'::character varying, 'PENDING'::character varying, 'PAID'::character varying, 'CANCELLED'::character varying, 'FAILED'::character varying, 'REFUNDED'::character varying]::text[])),
  currency_code character varying NOT NULL DEFAULT 'VND'::character varying CHECK (length(currency_code::text) = 3),
  subtotal_amount numeric NOT NULL DEFAULT 0,
  tax_amount numeric NOT NULL DEFAULT 0,
  discount_amount numeric NOT NULL DEFAULT 0,
  credit_applied numeric NOT NULL DEFAULT 0,
  total_amount numeric NOT NULL DEFAULT 0,
  items_snapshot jsonb NOT NULL DEFAULT '[]'::jsonb,
  billing_info jsonb NOT NULL DEFAULT '{}'::jsonb,
  payment_method character varying,
  payment_ref_id character varying,
  version bigint NOT NULL DEFAULT 1,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  deleted_at timestamp with time zone,
  CONSTRAINT subscription_orders_pkey PRIMARY KEY (_id),
  CONSTRAINT fk_order_tenant FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id),
  CONSTRAINT fk_order_user FOREIGN KEY (created_by) REFERENCES public.users(_id)
);
CREATE TABLE public.system_announcements (
  _id uuid NOT NULL DEFAULT gen_random_uuid(),
  tenant_id uuid NOT NULL,
  title character varying NOT NULL,
  content text NOT NULL,
  type character varying NOT NULL DEFAULT 'info'::character varying,
  priority character varying NOT NULL DEFAULT 'normal'::character varying,
  category character varying,
  status character varying NOT NULL DEFAULT 'draft'::character varying,
  is_published boolean DEFAULT false,
  is_pinned boolean DEFAULT false,
  start_date timestamp with time zone,
  end_date timestamp with time zone,
  published_at timestamp with time zone,
  target_audience jsonb DEFAULT '{"all": true}'::jsonb,
  display_location ARRAY DEFAULT ARRAY['dashboard'::text],
  icon character varying,
  color character varying,
  link_url character varying,
  link_text character varying,
  attachments jsonb,
  metadata jsonb,
  view_count integer DEFAULT 0,
  click_count integer DEFAULT 0,
  created_at timestamp with time zone DEFAULT now(),
  created_by character varying,
  updated_at timestamp with time zone DEFAULT now(),
  updated_by character varying,
  deleted_at timestamp with time zone,
  deleted_by character varying,
  version integer DEFAULT 1,
  CONSTRAINT system_announcements_pkey PRIMARY KEY (_id)
);
CREATE TABLE public.system_categories (
  _id uuid NOT NULL DEFAULT gen_random_uuid(),
  tenant_id uuid NOT NULL,
  type character varying NOT NULL,
  code character varying NOT NULL,
  name character varying NOT NULL,
  status smallint NOT NULL DEFAULT 1 CHECK (status = ANY (ARRAY[0, 1])),
  order integer DEFAULT 0,
  description text,
  parent_id character varying,
  group_category_id character varying,
  collection_name character varying DEFAULT 'system_categories'::character varying,
  extra_fields jsonb DEFAULT '[]'::jsonb,
  metadata jsonb DEFAULT '{}'::jsonb,
  is_system boolean DEFAULT false,
  is_editable boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  version integer DEFAULT 1,
  CONSTRAINT system_categories_pkey PRIMARY KEY (_id)
);
CREATE TABLE public.system_jobs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  job_name character varying NOT NULL,
  job_type character varying NOT NULL,
  description text,
  status character varying NOT NULL DEFAULT 'pending'::character varying,
  priority character varying NOT NULL DEFAULT 'normal'::character varying,
  schedule_type character varying,
  cron_expression character varying,
  last_run_at timestamp with time zone,
  next_run_at timestamp with time zone,
  last_run_duration integer,
  last_run_status character varying,
  last_run_error text,
  run_count integer DEFAULT 0,
  success_count integer DEFAULT 0,
  failure_count integer DEFAULT 0,
  is_active boolean DEFAULT true,
  created_by character varying,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT system_jobs_pkey PRIMARY KEY (id)
);
CREATE TABLE public.tags (
  _id uuid NOT NULL,
  tenant_id uuid NOT NULL,
  name text NOT NULL,
  slug character varying NOT NULL CHECK (slug::text ~ '^[a-z0-9-]+$'::text),
  description text,
  color character varying CHECK (color IS NULL OR color::text ~ '^#[A-Fa-f0-9]{6}$'::text),
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  usage_count bigint NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  version bigint NOT NULL DEFAULT 1 CHECK (version >= 1),
  CONSTRAINT tags_pkey PRIMARY KEY (_id),
  CONSTRAINT fk_tags_tenant FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id)
);
CREATE TABLE public.tenant_app_routes (
  _id uuid NOT NULL,
  tenant_id uuid NOT NULL,
  app_code character varying NOT NULL,
  domain character varying NOT NULL CHECK (domain::text ~ '^[a-z0-9.-]+$'::text),
  path_prefix character varying NOT NULL DEFAULT '/'::character varying CHECK (path_prefix::text ~ '^/[-a-z0-9/]*$'::text),
  is_primary boolean NOT NULL DEFAULT false,
  is_custom_domain boolean NOT NULL DEFAULT false,
  ssl_status character varying NOT NULL DEFAULT 'NONE'::character varying CHECK (ssl_status::text = ANY (ARRAY['NONE'::character varying, 'PENDING'::character varying, 'ACTIVE'::character varying, 'FAILED'::character varying]::text[])),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  version bigint NOT NULL DEFAULT 1,
  status character varying NOT NULL DEFAULT 'ACTIVE'::character varying CHECK (status::text = ANY (ARRAY['ACTIVE'::character varying, 'INACTIVE'::character varying, 'MAINTENANCE'::character varying, 'PENDING_DNS'::character varying]::text[])),
  route_scope character varying NOT NULL DEFAULT 'SPECIFIC_DOMAIN'::character varying CHECK (route_scope::text = ANY (ARRAY['SPECIFIC_DOMAIN'::character varying, 'ALL_MY_DOMAINS'::character varying, 'INHERITED'::character varying]::text[])),
  CONSTRAINT tenant_app_routes_pkey PRIMARY KEY (_id),
  CONSTRAINT fk_routes_tenant FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id)
);
CREATE TABLE public.tenant_applications (
  _id uuid NOT NULL DEFAULT gen_random_uuid(),
  tenant_id uuid NOT NULL,
  app_code character varying NOT NULL,
  is_active boolean NOT NULL DEFAULT true,
  activated_at timestamp with time zone,
  deactivated_at timestamp with time zone,
  license_type character varying DEFAULT 'TRIAL'::character varying CHECK (license_type::text = ANY (ARRAY['TRIAL'::character varying, 'BASIC'::character varying, 'PREMIUM'::character varying, 'ENTERPRISE'::character varying]::text[])),
  max_users integer DEFAULT 10 CHECK (max_users > 0),
  expires_at timestamp with time zone,
  settings jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  version bigint NOT NULL DEFAULT 1 CHECK (version >= 1),
  CONSTRAINT tenant_applications_pkey PRIMARY KEY (_id),
  CONSTRAINT fk_tenant_app_tenant FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id),
  CONSTRAINT fk_tenant_app_application FOREIGN KEY (app_code) REFERENCES public.applications(code)
);
CREATE TABLE public.tenant_digital_assets (
  _id uuid NOT NULL,
  tenant_id uuid NOT NULL,
  order_id uuid,
  asset_type character varying NOT NULL,
  name text NOT NULL,
  status character varying NOT NULL DEFAULT 'PENDING'::character varying CHECK (status::text = ANY (ARRAY['PENDING'::character varying, 'PROVISIONING'::character varying, 'ACTIVE'::character varying, 'EXPIRED'::character varying, 'SUSPENDED'::character varying, 'TRANSFERRING'::character varying]::text[])),
  auto_renew boolean NOT NULL DEFAULT true,
  asset_metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  activated_at timestamp with time zone,
  expires_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  version bigint NOT NULL DEFAULT 1 CHECK (version >= 1),
  CONSTRAINT tenant_digital_assets_pkey PRIMARY KEY (_id),
  CONSTRAINT fk_asset_tenant FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id),
  CONSTRAINT fk_asset_order FOREIGN KEY (order_id) REFERENCES public.subscription_orders(_id)
);
CREATE TABLE public.tenant_domains (
  _id uuid NOT NULL,
  tenant_id uuid NOT NULL,
  domain character varying NOT NULL UNIQUE CHECK (domain::text ~ '^[a-z0-9.-]+$'::text),
  verification_status character varying NOT NULL DEFAULT 'PENDING'::character varying CHECK (verification_status::text = ANY (ARRAY['PENDING'::character varying, 'VERIFIED'::character varying]::text[])),
  verification_method character varying CHECK (verification_method::text = ANY (ARRAY['DNS_TXT'::character varying, 'HTML_FILE'::character varying]::text[])),
  verification_token character varying,
  policy character varying NOT NULL DEFAULT 'NONE'::character varying CHECK (policy::text = ANY (ARRAY['NONE'::character varying, 'CAPTURE'::character varying, 'ENFORCE_SSO'::character varying]::text[])),
  verified_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tenant_domains_pkey PRIMARY KEY (_id),
  CONSTRAINT fk_domain_tenant FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id)
);
CREATE TABLE public.tenant_invitations (
  _id uuid NOT NULL,
  tenant_id uuid NOT NULL,
  email character varying NOT NULL CHECK (email::text ~* '^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$'::text),
  role_ids ARRAY DEFAULT '{}'::text[],
  department_id uuid,
  token character varying NOT NULL UNIQUE,
  status character varying NOT NULL DEFAULT 'PENDING'::character varying CHECK (status::text = ANY (ARRAY['PENDING'::character varying, 'ACCEPTED'::character varying, 'EXPIRED'::character varying, 'REVOKED'::character varying]::text[])),
  expires_at timestamp with time zone NOT NULL,
  invited_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tenant_invitations_pkey PRIMARY KEY (_id),
  CONSTRAINT fk_invitation_tenant FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id)
);
CREATE TABLE public.tenant_members (
  _id uuid NOT NULL DEFAULT uuid_generate_v4(),
  tenant_id uuid NOT NULL,
  user_id uuid NOT NULL,
  employee_code character varying,
  internal_email character varying,
  job_title character varying,
  manager_id uuid,
  role character varying NOT NULL DEFAULT 'MEMBER'::character varying CHECK (role::text = ANY (ARRAY['OWNER'::character varying, 'ADMIN'::character varying, 'MEMBER'::character varying, 'VIEWER'::character varying]::text[])),
  status character varying NOT NULL DEFAULT 'ACTIVE'::character varying CHECK (status::text = ANY (ARRAY['ACTIVE'::character varying, 'RESIGNED'::character varying, 'ONBOARDING'::character varying, 'SUSPENDED'::character varying]::text[])),
  joined_at timestamp with time zone,
  left_at timestamp with time zone,
  permissions jsonb DEFAULT '[]'::jsonb,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  deleted_at timestamp with time zone,
  created_by uuid,
  updated_by uuid,
  deleted_by uuid,
  version bigint NOT NULL DEFAULT 1 CHECK (version >= 1),
  CONSTRAINT tenant_members_pkey PRIMARY KEY (_id),
  CONSTRAINT fk_tenant_members_tenant FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id),
  CONSTRAINT fk_tenant_members_manager FOREIGN KEY (manager_id) REFERENCES public.tenant_members(_id)
);
CREATE TABLE public.tenant_rate_limits (
  _id uuid NOT NULL DEFAULT gen_random_uuid(),
  tenant_id uuid NOT NULL,
  service_package_id uuid,
  limit_name character varying NOT NULL,
  limit_key character varying NOT NULL,
  resource_type character varying CHECK ((resource_type::text = ANY (ARRAY['api'::character varying, 'storage'::character varying, 'database'::character varying, 'compute'::character varying, 'network'::character varying, 'email'::character varying, 'sms'::character varying]::text[])) OR resource_type IS NULL),
  endpoint_pattern character varying,
  max_requests integer NOT NULL CHECK (max_requests > 0),
  time_window integer NOT NULL CHECK (time_window > 0),
  window_unit character varying DEFAULT 'second'::character varying CHECK (window_unit::text = ANY (ARRAY['second'::character varying, 'minute'::character varying, 'hour'::character varying, 'day'::character varying, 'month'::character varying]::text[])),
  burst_limit integer,
  concurrent_limit integer CHECK (concurrent_limit IS NULL OR concurrent_limit > 0),
  limit_type character varying DEFAULT 'sliding_window'::character varying CHECK (limit_type::text = ANY (ARRAY['sliding_window'::character varying, 'fixed_window'::character varying, 'token_bucket'::character varying, 'leaky_bucket'::character varying]::text[])),
  limit_scope character varying DEFAULT 'tenant'::character varying CHECK (limit_scope::text = ANY (ARRAY['tenant'::character varying, 'user'::character varying, 'ip'::character varying, 'api_key'::character varying, 'global'::character varying]::text[])),
  is_enabled boolean DEFAULT true,
  is_strict boolean DEFAULT true,
  block_duration integer,
  retry_after integer,
  custom_error_message text,
  custom_error_code character varying,
  current_usage integer DEFAULT 0 CHECK (current_usage >= 0),
  peak_usage integer DEFAULT 0 CHECK (peak_usage >= 0),
  last_exceeded_at timestamp with time zone,
  exceeded_count integer DEFAULT 0 CHECK (exceeded_count >= 0),
  alert_threshold integer CHECK (alert_threshold IS NULL OR alert_threshold > 0 AND alert_threshold <= 100),
  alert_enabled boolean DEFAULT false,
  priority integer DEFAULT 0 CHECK (priority >= 0),
  can_override boolean DEFAULT false,
  override_until timestamp with time zone,
  description text,
  tags ARRAY,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT tenant_rate_limits_pkey PRIMARY KEY (_id),
  CONSTRAINT fk_tenant_rate_limits_created_by_users FOREIGN KEY (created_by) REFERENCES public.users(_id),
  CONSTRAINT fk_tenant_rate_limits_updated_by_users FOREIGN KEY (updated_by) REFERENCES public.users(_id),
  CONSTRAINT tenant_rate_limits_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id),
  CONSTRAINT tenant_rate_limits_service_package_id_fkey FOREIGN KEY (service_package_id) REFERENCES public.service_packages(_id)
);
CREATE TABLE public.tenant_service_deliveries (
  _id uuid NOT NULL,
  tenant_id uuid NOT NULL,
  product_id uuid NOT NULL,
  subscription_id uuid,
  unit_type character varying NOT NULL,
  total_units numeric NOT NULL DEFAULT 0,
  delivered_units numeric NOT NULL DEFAULT 0,
  unit_price numeric NOT NULL DEFAULT 0,
  currency_code character varying NOT NULL DEFAULT 'VND'::character varying,
  status character varying NOT NULL DEFAULT 'PENDING'::character varying CHECK (status::text = ANY (ARRAY['PENDING'::character varying, 'IN_PROGRESS'::character varying, 'COMPLETED'::character varying, 'CANCELLED'::character varying]::text[])),
  service_metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  version bigint NOT NULL DEFAULT 1 CHECK (version >= 1),
  CONSTRAINT tenant_service_deliveries_pkey PRIMARY KEY (_id),
  CONSTRAINT fk_service_tenant FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id),
  CONSTRAINT fk_service_product FOREIGN KEY (product_id) REFERENCES public.saas_products(_id),
  CONSTRAINT fk_service_sub FOREIGN KEY (subscription_id) REFERENCES public.tenant_subscriptions(_id)
);
CREATE TABLE public.tenant_sso_configs (
  _id uuid NOT NULL DEFAULT gen_random_uuid(),
  tenant_id uuid NOT NULL,
  provider character varying NOT NULL CHECK (provider::text = ANY (ARRAY['SAML'::character varying, 'OAUTH2'::character varying, 'OIDC'::character varying, 'LDAP'::character varying, 'CAS'::character varying, 'OTHER'::character varying]::text[])),
  name character varying NOT NULL,
  description text,
  status character varying NOT NULL DEFAULT 'ACTIVE'::character varying CHECK (status::text = ANY (ARRAY['ACTIVE'::character varying, 'INACTIVE'::character varying, 'TESTING'::character varying, 'DEPRECATED'::character varying]::text[])),
  entity_id character varying,
  sso_url text,
  slo_url text,
  certificate text,
  metadata_url text,
  client_id character varying,
  client_secret text,
  authorization_endpoint text,
  token_endpoint text,
  userinfo_endpoint text,
  jwks_uri text,
  scopes jsonb DEFAULT '[]'::jsonb,
  attribute_mapping jsonb DEFAULT '{}'::jsonb,
  settings jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT tenant_sso_configs_pkey PRIMARY KEY (_id),
  CONSTRAINT tenant_sso_configs_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id)
);
CREATE TABLE public.tenant_subscriptions (
  _id uuid NOT NULL DEFAULT gen_random_uuid(),
  tenant_id uuid NOT NULL,
  plan_id uuid,
  order_id uuid,
  subscription_number character varying NOT NULL UNIQUE,
  subscription_name character varying NOT NULL,
  start_date date NOT NULL,
  end_date date NOT NULL,
  trial_end_date date,
  renewal_date date,
  status character varying NOT NULL DEFAULT 'active'::character varying CHECK (status::text = ANY (ARRAY['active'::character varying, 'trial'::character varying, 'suspended'::character varying, 'expired'::character varying, 'cancelled'::character varying, 'pending'::character varying]::text[])),
  auto_renew boolean DEFAULT true,
  is_trial boolean DEFAULT false,
  plan_name character varying,
  billing_cycle character varying NOT NULL DEFAULT 'monthly'::character varying CHECK (billing_cycle::text = ANY (ARRAY['monthly'::character varying, 'quarterly'::character varying, 'yearly'::character varying, 'custom'::character varying]::text[])),
  base_price numeric NOT NULL DEFAULT 0,
  discount_amount numeric DEFAULT 0,
  tax_amount numeric DEFAULT 0,
  total_amount numeric NOT NULL DEFAULT 0,
  currency character varying DEFAULT 'USD'::character varying,
  max_users integer DEFAULT 1,
  current_users integer DEFAULT 0,
  max_storage_gb integer DEFAULT 10,
  current_storage_gb numeric DEFAULT 0,
  features jsonb DEFAULT '[]'::jsonb,
  limits jsonb DEFAULT '{}'::jsonb,
  payment_method character varying,
  payment_status character varying DEFAULT 'unpaid'::character varying CHECK (payment_status::text = ANY (ARRAY['paid'::character varying, 'unpaid'::character varying, 'partially_paid'::character varying, 'failed'::character varying, 'refunded'::character varying]::text[])),
  last_payment_date date,
  next_payment_date date,
  billing_contact_name character varying,
  billing_contact_email character varying,
  billing_contact_phone character varying,
  notes text,
  metadata jsonb DEFAULT '{}'::jsonb,
  tags ARRAY,
  created_at timestamp with time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  created_by uuid,
  updated_at timestamp with time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_by uuid,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT tenant_subscriptions_pkey PRIMARY KEY (_id)
);
CREATE TABLE public.tenants (
  _id uuid NOT NULL DEFAULT gen_random_uuid(),
  code character varying NOT NULL UNIQUE CHECK (code::text ~ '^[a-z0-9-]+$'::text),
  name character varying NOT NULL,
  parent_tenant_id uuid,
  path text,
  tier character varying NOT NULL DEFAULT 'FREE'::character varying CHECK (tier::text = ANY (ARRAY['FREE'::character varying, 'PRO'::character varying, 'ENTERPRISE'::character varying, 'PARTNER_BASIC'::character varying, 'PARTNER_PREMIUM'::character varying, 'PARTNER_ELITE'::character varying, 'PROVIDER'::character varying]::text[])),
  status character varying NOT NULL DEFAULT 'TRIAL'::character varying CHECK (status::text = ANY (ARRAY['TRIAL'::character varying, 'ACTIVE'::character varying, 'SUSPENDED'::character varying, 'CANCELLED'::character varying]::text[])),
  data_region character varying NOT NULL DEFAULT 'ap-southeast-1'::character varying CHECK (data_region::text = ANY (ARRAY['ap-southeast-1'::character varying, 'us-east-1'::character varying, 'eu-central-1'::character varying]::text[])),
  compliance_level character varying NOT NULL DEFAULT 'STANDARD'::character varying CHECK (compliance_level::text = ANY (ARRAY['STANDARD'::character varying, 'GDPR'::character varying, 'HIPAA'::character varying, 'PCI-DSS'::character varying]::text[])),
  timezone character varying NOT NULL DEFAULT 'UTC'::character varying,
  billing_type character varying NOT NULL DEFAULT 'POSTPAID'::character varying CHECK (billing_type::text = ANY (ARRAY['PREPAID'::character varying, 'POSTPAID'::character varying]::text[])),
  profile jsonb NOT NULL DEFAULT '{}'::jsonb,
  settings jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  version bigint NOT NULL DEFAULT 1 CHECK (version >= 1),
  partner_tenant_id uuid,
  CONSTRAINT tenants_pkey PRIMARY KEY (_id),
  CONSTRAINT tenants_parent_tenant_id_fkey FOREIGN KEY (parent_tenant_id) REFERENCES public.tenants(_id),
  CONSTRAINT fk_tenant_parent FOREIGN KEY (parent_tenant_id) REFERENCES public.tenants(_id),
  CONSTRAINT fk_tenant_partner FOREIGN KEY (partner_tenant_id) REFERENCES public.tenants(_id)
);
CREATE TABLE public.usage_events (
  _id uuid NOT NULL,
  tenant_id uuid,
  subscription_id uuid,
  app_code text,
  event_type text,
  quantity numeric,
  unit text,
  metadata jsonb,
  data_region text,
  timestamp timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT usage_events_pkey PRIMARY KEY (_id)
);
CREATE TABLE public.user_consents (
  _id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  legal_document_id uuid NOT NULL,
  consent_given boolean DEFAULT true,
  consent_date timestamp with time zone DEFAULT now(),
  consent_ip character varying,
  consent_user_agent text,
  consent_method character varying CHECK (consent_method::text = ANY (ARRAY['web'::character varying, 'mobile'::character varying, 'api'::character varying, 'email'::character varying, 'signup'::character varying, 'profile'::character varying, 'checkout'::character varying, 'other'::character varying]::text[])),
  document_version character varying,
  document_title character varying,
  document_type character varying,
  withdrawn boolean DEFAULT false,
  withdrawn_date timestamp with time zone,
  withdrawn_reason text,
  expires_at timestamp with time zone,
  renewal_required boolean DEFAULT false,
  last_renewed_at timestamp with time zone,
  source_application character varying,
  source_page character varying,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT user_consents_pkey PRIMARY KEY (_id),
  CONSTRAINT user_consents_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(_id)
);
CREATE TABLE public.user_delegations (
  _id uuid NOT NULL DEFAULT gen_random_uuid(),
  delegator_id uuid NOT NULL,
  delegate_id uuid NOT NULL,
  tenant_id uuid,
  scope character varying CHECK (scope::text = ANY (ARRAY['admin'::character varying, 'manager'::character varying, 'editor'::character varying, 'viewer'::character varying, 'approver'::character varying, 'reviewer'::character varying, 'auditor'::character varying, 'custom'::character varying]::text[])),
  permissions jsonb DEFAULT '[]'::jsonb,
  reason text,
  notes text,
  start_date timestamp with time zone NOT NULL DEFAULT now(),
  end_date timestamp with time zone,
  status character varying DEFAULT 'active'::character varying CHECK (status::text = ANY (ARRAY['pending'::character varying, 'active'::character varying, 'expired'::character varying, 'revoked'::character varying, 'suspended'::character varying]::text[])),
  activated_at timestamp with time zone,
  revoked_at timestamp with time zone,
  revoked_by uuid,
  revoked_reason text,
  auto_expire boolean DEFAULT true,
  notified_before_expiry boolean DEFAULT false,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT user_delegations_pkey PRIMARY KEY (_id),
  CONSTRAINT user_delegations_delegator_id_fkey FOREIGN KEY (delegator_id) REFERENCES public.users(_id),
  CONSTRAINT user_delegations_delegate_id_fkey FOREIGN KEY (delegate_id) REFERENCES public.users(_id),
  CONSTRAINT user_delegations_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id),
  CONSTRAINT user_delegations_revoked_by_fkey FOREIGN KEY (revoked_by) REFERENCES public.users(_id)
);
CREATE TABLE public.user_devices (
  _id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  device_type character varying NOT NULL CHECK (device_type::text = ANY (ARRAY['desktop'::character varying, 'mobile'::character varying, 'tablet'::character varying, 'watch'::character varying, 'tv'::character varying, 'other'::character varying]::text[])),
  device_name character varying,
  device_model character varying,
  manufacturer character varying,
  os character varying CHECK (os::text = ANY (ARRAY['windows'::character varying, 'macos'::character varying, 'linux'::character varying, 'ios'::character varying, 'android'::character varying, 'chromeos'::character varying, 'other'::character varying]::text[])),
  os_version character varying,
  browser character varying CHECK (browser::text = ANY (ARRAY['chrome'::character varying, 'firefox'::character varying, 'safari'::character varying, 'edge'::character varying, 'opera'::character varying, 'brave'::character varying, 'samsung'::character varying, 'other'::character varying]::text[])),
  browser_version character varying,
  app_name character varying,
  app_version character varying,
  ip_address inet,
  user_agent text,
  location jsonb DEFAULT '{}'::jsonb,
  is_trusted boolean DEFAULT false,
  fingerprint character varying,
  push_token text,
  first_seen_at timestamp with time zone DEFAULT now(),
  last_used_at timestamp with time zone DEFAULT now(),
  login_count integer DEFAULT 1,
  status character varying DEFAULT 'active'::character varying CHECK (status::text = ANY (ARRAY['active'::character varying, 'inactive'::character varying, 'blocked'::character varying, 'revoked'::character varying]::text[])),
  revoked_at timestamp with time zone,
  revoked_reason text,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT user_devices_pkey PRIMARY KEY (_id),
  CONSTRAINT user_devices_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(_id)
);
CREATE TABLE public.user_groups (
  _id uuid NOT NULL DEFAULT uuid_generate_v4(),
  tenant_id uuid NOT NULL,
  code character varying NOT NULL,
  name character varying NOT NULL,
  description text,
  group_type character varying,
  status character varying NOT NULL DEFAULT 'ACTIVE'::character varying CHECK (status::text = ANY (ARRAY['ACTIVE'::character varying, 'INACTIVE'::character varying, 'ARCHIVED'::character varying]::text[])),
  order integer DEFAULT 0,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  deleted_at timestamp with time zone,
  created_by uuid,
  updated_by uuid,
  deleted_by uuid,
  version bigint NOT NULL DEFAULT 1 CHECK (version >= 1),
  CONSTRAINT user_groups_pkey PRIMARY KEY (_id),
  CONSTRAINT fk_user_groups_tenant FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id)
);
create table public.auth_identifiers (
  tenant_id uuid not null,
  identifier_hash bytea not null,
  user_id uuid not null,
  identity_id uuid not null,
  identifier_type character varying(20) not null,
  original_value text null,
  constraint auth_identifiers_pkey primary key (tenant_id, identifier_hash),
  constraint fk_auth_id_user foreign KEY (user_id) references users (_id) on delete CASCADE
) TABLESPACE pg_default;

create table public.user_identities (
  _id uuid not null,
  user_id uuid not null,
  identity_type character varying(20) not null,
  identity_value text not null,
  credential_secret text null,
  metadata jsonb not null default '{}'::jsonb,
  is_verified boolean not null default false,
  verified_at timestamp with time zone null,
  last_login_at timestamp with time zone null,
  created_at timestamp with time zone not null default now(),
  updated_at timestamp with time zone not null default now(),
  version bigint not null default 1,
  constraint user_identities_pkey primary key (_id),
  constraint uq_identity_source unique (identity_type, identity_value),
  constraint fk_ui_user foreign KEY (user_id) references users (_id) on delete CASCADE,
  constraint chk_ui_dates check ((updated_at >= created_at)),
  constraint chk_ui_type check (
    (
      (identity_type)::text = any (
        (
          array[
            'PASSWORD'::character varying,
            'GOOGLE'::character varying,
            'GITHUB'::character varying,
            'MICROSOFT'::character varying,
            'APPLE'::character varying,
            'SAML'::character varying,
            'OIDC'::character varying
          ]
        )::text[]
      )
    )
  ),
  constraint chk_ui_version check ((version >= 1))
) TABLESPACE pg_default;
CREATE TABLE public.user_mfa_methods (
  _id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  method_type character varying NOT NULL CHECK (method_type::text = ANY (ARRAY['TOTP'::character varying, 'SMS'::character varying, 'EMAIL'::character varying, 'WEBAUTHN'::character varying, 'BACKUP_CODES'::character varying, 'PUSH_NOTIFICATION'::character varying, 'BIOMETRIC'::character varying, 'HARDWARE_TOKEN'::character varying, 'OTHER'::character varying]::text[])),
  method_name character varying,
  sms_phone_number character varying,
  sms_phone_verified boolean DEFAULT false,
  email_address character varying,
  email_verified boolean DEFAULT false,
  status character varying NOT NULL DEFAULT 'PENDING'::character varying CHECK (status::text = ANY (ARRAY['ACTIVE'::character varying, 'INACTIVE'::character varying, 'SUSPENDED'::character varying, 'REVOKED'::character varying, 'PENDING'::character varying]::text[])),
  is_verified boolean NOT NULL DEFAULT false,
  is_primary boolean NOT NULL DEFAULT false,
  is_enforced boolean NOT NULL DEFAULT false,
  last_used_at timestamp with time zone,
  last_verified_at timestamp with time zone,
  success_count integer NOT NULL DEFAULT 0,
  failure_count integer NOT NULL DEFAULT 0,
  device_name character varying,
  device_type character varying,
  backup_codes_used integer DEFAULT 0,
  backup_codes_total integer DEFAULT 10,
  totp_secret_encrypted text,
  totp_backup_codes_encrypted text,
  backup_codes_encrypted text,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT user_mfa_methods_pkey PRIMARY KEY (_id)
);
CREATE TABLE public.user_roles (
  _id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  role_id uuid NOT NULL,
  tenant_id uuid,
  scope character varying NOT NULL DEFAULT 'global'::character varying,
  scope_id uuid,
  granted_by uuid,
  granted_at timestamp with time zone DEFAULT now(),
  expires_at timestamp with time zone,
  is_active boolean DEFAULT true,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT user_roles_pkey PRIMARY KEY (_id),
  CONSTRAINT user_roles_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(_id),
  CONSTRAINT user_roles_role_id_fkey FOREIGN KEY (role_id) REFERENCES public.roles(_id),
  CONSTRAINT user_roles_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id),
  CONSTRAINT user_roles_granted_by_fkey FOREIGN KEY (granted_by) REFERENCES public.users(_id)
);
CREATE TABLE public.user_sessions (
  _id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  session_token character varying NOT NULL UNIQUE,
  device_name character varying,
  device_type character varying,
  browser character varying,
  os character varying,
  ip_address inet,
  location character varying,
  is_active boolean DEFAULT true,
  last_activity_at timestamp with time zone DEFAULT now(),
  expires_at timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT user_sessions_pkey PRIMARY KEY (_id),
  CONSTRAINT user_sessions_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(_id)
);
CREATE TABLE public.users (
  _id uuid NOT NULL DEFAULT gen_random_uuid(),
  email character varying NOT NULL CHECK (email::text ~* '^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$'::text),
  password_hash text,
  full_name text NOT NULL,
  avatar_url text CHECK (avatar_url IS NULL OR avatar_url ~* '^https?://'::text),
  phone_number character varying UNIQUE,
  status character varying NOT NULL DEFAULT 'ACTIVE'::character varying CHECK (status::text = ANY (ARRAY['ACTIVE'::character varying, 'BANNED'::character varying, 'DISABLED'::character varying, 'PENDING'::character varying]::text[])),
  is_support_staff boolean NOT NULL DEFAULT false,
  mfa_enabled boolean NOT NULL DEFAULT false,
  mfa_secret text,
  is_verified boolean NOT NULL DEFAULT false,
  locale character varying NOT NULL DEFAULT 'vi-VN'::character varying,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  deleted_at timestamp with time zone,
  CONSTRAINT users_pkey PRIMARY KEY (_id)
);
CREATE TABLE public.webhooks (
  _id uuid NOT NULL DEFAULT gen_random_uuid(),
  tenant_id uuid NOT NULL,
  name character varying NOT NULL,
  description text,
  url text NOT NULL,
  method character varying DEFAULT 'POST'::character varying CHECK (method::text = ANY (ARRAY['POST'::character varying, 'GET'::character varying, 'PUT'::character varying, 'PATCH'::character varying, 'DELETE'::character varying]::text[])),
  event_types ARRAY NOT NULL DEFAULT '{}'::text[] CHECK (array_length(event_types, 1) > 0),
  event_filter jsonb,
  secret_key text,
  auth_type character varying DEFAULT 'none'::character varying CHECK (auth_type::text = ANY (ARRAY['none'::character varying, 'basic'::character varying, 'bearer'::character varying, 'api_key'::character varying, 'oauth2'::character varying]::text[])),
  auth_config jsonb,
  headers jsonb DEFAULT '{}'::jsonb,
  timeout_ms integer DEFAULT 5000 CHECK (timeout_ms > 0 AND timeout_ms <= 60000),
  retry_config jsonb DEFAULT '{"max_retries": 3, "retry_delay": 1000, "backoff_multiplier": 2}'::jsonb,
  is_active boolean DEFAULT true,
  is_verified boolean DEFAULT false,
  verification_token text,
  verified_at timestamp with time zone,
  last_triggered_at timestamp with time zone,
  last_success_at timestamp with time zone,
  last_failure_at timestamp with time zone,
  success_count integer DEFAULT 0 CHECK (success_count >= 0),
  failure_count integer DEFAULT 0 CHECK (failure_count >= 0),
  total_count integer DEFAULT 0 CHECK (total_count >= 0),
  avg_response_time_ms integer,
  batch_size integer CHECK (batch_size IS NULL OR batch_size > 0),
  rate_limit integer CHECK (rate_limit IS NULL OR rate_limit > 0),
  priority integer DEFAULT 0 CHECK (priority >= 0),
  tags ARRAY,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT webhooks_pkey PRIMARY KEY (_id),
  CONSTRAINT webhooks_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(_id)
);
-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE telemetry.api_usage_logs (
  _id uuid NOT NULL,
  tenant_id uuid,
  app_code text,
  api_endpoint text,
  api_method text,
  status_code smallint,
  request_size bigint DEFAULT 0,
  response_size bigint DEFAULT 0,
  latency_ms integer,
  api_key_id uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT api_usage_logs_pkey PRIMARY KEY (_id)
);
CREATE TABLE telemetry.audit_logs (
  _id uuid NOT NULL,
  tenant_id uuid,
  user_id uuid,
  impersonator_id uuid,
  event_time timestamp with time zone NOT NULL DEFAULT now(),
  action text,
  resource text,
  resource_id text,
  details jsonb,
  ip_address text,
  user_agent text,
  status text,
  CONSTRAINT audit_logs_pkey PRIMARY KEY (_id)
);
CREATE TABLE telemetry.content_view_logs (
  _id uuid NOT NULL,
  tenant_id uuid NOT NULL,
  object_type USER-DEFINED NOT NULL,
  object_id uuid NOT NULL,
  category_ids ARRAY NOT NULL DEFAULT ARRAY[]::uuid[],
  author_id uuid,
  user_id uuid,
  visitor_id text NOT NULL,
  ip_address inet NOT NULL,
  user_agent text NOT NULL,
  device_type USER-DEFINED NOT NULL,
  referrer text,
  view_duration integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT content_view_logs_pkey PRIMARY KEY (_id)
);
CREATE TABLE telemetry.saas_business_reports (
  _id uuid NOT NULL,
  report_date date,
  partner_id uuid,
  revenue_category text,
  total_revenue numeric,
  currency_code character DEFAULT 'VND'::bpchar,
  tenant_count integer,
  details_json jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT saas_business_reports_pkey PRIMARY KEY (_id)
);
CREATE TABLE telemetry.traffic_logs (
  _id uuid NOT NULL,
  tenant_id uuid,
  user_id uuid,
  app_code text,
  method text,
  domain text,
  path text,
  status_code smallint,
  latency_ms integer,
  request_size bigint DEFAULT 0,
  response_size bigint DEFAULT 0,
  ip_address inet,
  user_agent text,
  data_region text DEFAULT 'ap-southeast-1'::text,
  timestamp timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT traffic_logs_pkey PRIMARY KEY (_id)
);
CREATE TABLE telemetry.user_registration_logs (
  _id uuid NOT NULL,
  tenant_id uuid,
  user_id uuid,
  registration_source text,
  data_region text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT user_registration_logs_pkey PRIMARY KEY (_id)
);
CREATE TABLE telemetry.webhook_delivery_logs (
  _id uuid NOT NULL,
  tenant_id uuid,
  webhook_id uuid,
  event_type text,
  target_url text,
  payload jsonb,
  response_body text,
  status_code smallint,
  is_success boolean,
  latency_ms integer,
  attempt_number smallint DEFAULT 1,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT webhook_delivery_logs_pkey PRIMARY KEY (_id)
);

